'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var react = require('@xstate/react');

function _interopDefault (e) { return e && e.__esModule ? e : { 'default': e }; }

var React__default = /*#__PURE__*/_interopDefault(React);

const Connect2ICContext = /*#__PURE__*/React.createContext({});

const Connect2ICProvider = _ref => {
  let {
    children,
    client
  } = _ref;
  const [open, setOpen] = React.useState(false);
  const dialog = {
    open: () => setOpen(true),
    close: () => setOpen(false),
    isOpen: open
  };
  return /*#__PURE__*/React__default["default"].createElement(Connect2ICContext.Provider, {
    value: {
      client,
      dialog
    }
  }, children);
};

const useConnect = props => {
  var _client$_service$stat, _client$_service$stat2, _client$_service$stat3, _client$_service$stat4, _client$_service$stat5, _client$_service$stat6, _client$_service$stat7, _client$_service$stat8, _client$_service$stat9, _client$_service$stat10;

  // TODO: handle
  const {
    onConnect = () => {},
    onDisconnect = () => {}
  } = props !== null && props !== void 0 ? props : {};
  const {
    client
  } = React.useContext(Connect2ICContext);
  const {
    principal,
    activeProvider,
    status
  } = react.useSelector(client._service, state => ({
    principal: state.context.principal,
    activeProvider: state.context.activeProvider,
    status: state.value.idle
  }));
  React.useEffect(() => {
    const unsub = client.on("connect", onConnect);
    const unsub2 = client.on("disconnect", onDisconnect);
    return () => {
      unsub();
      unsub2();
    };
  }, [client]);
  return {
    principal,
    activeProvider,
    status,
    isInitializing: (_client$_service$stat = (_client$_service$stat2 = client._service.state) === null || _client$_service$stat2 === void 0 ? void 0 : _client$_service$stat2.matches({
      idle: "initializing"
    })) !== null && _client$_service$stat !== void 0 ? _client$_service$stat : false,
    isConnected: (_client$_service$stat3 = (_client$_service$stat4 = client._service.state) === null || _client$_service$stat4 === void 0 ? void 0 : _client$_service$stat4.matches({
      idle: "connected"
    })) !== null && _client$_service$stat3 !== void 0 ? _client$_service$stat3 : false,
    isConnecting: (_client$_service$stat5 = (_client$_service$stat6 = client._service.state) === null || _client$_service$stat6 === void 0 ? void 0 : _client$_service$stat6.matches({
      idle: "connecting"
    })) !== null && _client$_service$stat5 !== void 0 ? _client$_service$stat5 : false,
    isDisconnecting: (_client$_service$stat7 = (_client$_service$stat8 = client._service.state) === null || _client$_service$stat8 === void 0 ? void 0 : _client$_service$stat8.matches({
      idle: "disconnecting"
    })) !== null && _client$_service$stat7 !== void 0 ? _client$_service$stat7 : false,
    isIdle: (_client$_service$stat9 = (_client$_service$stat10 = client._service.state) === null || _client$_service$stat10 === void 0 ? void 0 : _client$_service$stat10.matches({
      idle: "idle"
    })) !== null && _client$_service$stat9 !== void 0 ? _client$_service$stat9 : false,
    connect: provider => {
      client.connect(provider);
    },
    disconnect: () => {
      client.disconnect();
    }
  };
};

// TODO: Figure out ts error
// @ts-ignore
const useCanister = function (canisterName) {
  let options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {
    mode: "auto" // "anonymous" | "connected"

  };
  const {
    mode
  } = options;
  const {
    client
  } = React.useContext(Connect2ICContext);
  const anonymousActor = react.useSelector(client._service, state => state.context.anonymousActors[canisterName]);
  const actor = react.useSelector(client._service, state => state.context.actors[canisterName]);
  const canisterDefinition = react.useSelector(client._service, state => state.context.canisters[canisterName]);
  const {
    isConnected
  } = useConnect();
  const signedIn = isConnected && actor && mode !== "anonymous";
  const canister = signedIn ? actor : anonymousActor; // TODO:

  const loading = !canister;
  const error = false;
  return [canister, {
    error,
    loading,
    canisterDefinition
  }];
};

const useWallet = () => {
  const {
    client
  } = React.useContext(Connect2ICContext);
  const activeProvider = react.useSelector(client._service, state => state.context.activeProvider);
  const supportsWallet = !!(activeProvider !== null && activeProvider !== void 0 && activeProvider.meta.features.includes("wallet"));
  const {
    isConnected
  } = useConnect();
  const wallet = isConnected && supportsWallet ? activeProvider : undefined;
  return [wallet];
};

const useBalance = () => {
  // TODO: check if supported or not
  const [wallet] = useWallet();
  const {
    activeProvider
  } = useConnect();
  const [assets, setAssets] = React.useState();
  const [loading, setLoading] = React.useState(true); // TODO:

  const [error, setError] = React.useState(false);

  const refetch = async () => {
    var _activeProvider$query;

    if (!wallet || !activeProvider) {
      setAssets(undefined);
      return;
    } // TODO: errors


    const result = await ((_activeProvider$query = activeProvider.queryBalance) === null || _activeProvider$query === void 0 ? void 0 : _activeProvider$query.call(activeProvider));
    setAssets(result);
    setLoading(false);
    return result;
  };

  React.useEffect(() => {
    if (!wallet) {
      setAssets(undefined);
      return;
    }

    refetch();
  }, [wallet]);
  return [assets, {
    loading,
    error,
    refetch
  }];
};

const useDialog = function () {
  const {
    dialog
  } = React.useContext(Connect2ICContext);
  return dialog;
};

const useProviders = () => {
  const {
    client
  } = React.useContext(Connect2ICContext);
  const providers = react.useSelector(client._service, state => state.context.providers);
  return providers !== null && providers !== void 0 ? providers : [];
};

const useSignMessage = _ref => {
  let {
    message
  } = _ref;
  // TODO: check if supported or not
  const {
    activeProvider
  } = useConnect();
  const [wallet] = useWallet();

  const signMessage = () => {
    var _activeProvider$signM;

    if (!wallet || !activeProvider) {
      return;
    }

    (_activeProvider$signM = activeProvider.signMessage) === null || _activeProvider$signM === void 0 ? void 0 : _activeProvider$signM.call(activeProvider, {
      message
    });
  };

  const loading = false;
  const error = false;
  return [signMessage, {
    loading,
    error
  }];
};

const useTransfer = _ref => {
  let {
    amount,
    to,
    from = undefined
  } = _ref;
  // TODO: check if supported or not
  const [wallet] = useWallet();
  const {
    activeProvider,
    principal
  } = useConnect();
  const [loading, setLoading] = React.useState(false);
  const [error, setError] = React.useState();

  const transfer = async () => {
    if (!wallet || !activeProvider) {
      return;
    }

    setLoading(true);
    const result = await activeProvider.requestTransfer({
      amount,
      to,
      from: from !== null && from !== void 0 ? from : principal
    }).catch(e => {
      setError(e);
    });
    setLoading(false);
    return result;
  };

  return [transfer, {
    loading,
    error
  }];
};

const useClient = () => {
  const {
    client
  } = React.useContext(Connect2ICContext);
  return client;
};

const ConnectButton = props => {
  const {
    style = {},
    dark = false,
    onConnect = () => {},
    onDisconnect = () => {},
    children
  } = props;
  const dialog = useDialog();
  const {
    disconnect,
    isConnected
  } = useConnect({
    onConnect,
    onDisconnect
  });
  return /*#__PURE__*/React__default["default"].createElement(React__default["default"].Fragment, null, !isConnected ? /*#__PURE__*/React__default["default"].createElement("button", {
    onClick: () => dialog.open(),
    style: style,
    className: "connect-button"
  }, children !== null && children !== void 0 ? children : "Connect") : null, isConnected ? /*#__PURE__*/React__default["default"].createElement("button", {
    onClick: disconnect,
    style: style,
    className: "connect-button"
  }, children !== null && children !== void 0 ? children : "Disconnect") : null);
};

var ConnectButton$1 = ConnectButton;

function _extends() {
  _extends = Object.assign || function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];

      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }

    return target;
  };

  return _extends.apply(this, arguments);
}

const ConnectDialog = props => {
  const {
    onClose = () => {
      dialog.close();
    },
    children,
    dark
  } = props;
  const dialog = useDialog();
  const providers = useProviders();
  const {
    connect,
    isConnected
  } = useConnect();
  React.useEffect(() => {
    if (isConnected) {
      dialog.close();
    }
  }, [isConnected]);
  React.useEffect(() => {
    if (dialog.isOpen) {
      document.body.style.overflow = "hidden";
    }

    if (!dialog.isOpen) {
      document.body.style.overflow = "unset";
    }
  }, [dialog.isOpen]);
  React.useEffect(() => {
    const handleEsc = event => {
      if (event.keyCode === 27) {
        dialog.close();
      }
    };

    window.addEventListener("keydown", handleEsc);
    return () => {
      window.removeEventListener("keydown", handleEsc);
    };
  }, []);

  const onClickInside = e => {
    e.stopPropagation();
  };

  return dialog.isOpen ? /*#__PURE__*/React__default["default"].createElement(React__default["default"].Fragment, null, /*#__PURE__*/React__default["default"].createElement("div", {
    className: "dialog-styles ".concat(dark ? "dark" : "light"),
    onClick: onClose
  }, /*#__PURE__*/React__default["default"].createElement("div", {
    onClick: onClickInside,
    className: "dialog-container"
  }, /*#__PURE__*/React__default["default"].createElement("div", null, providers.map(provider => {
    return /*#__PURE__*/React__default["default"].createElement("button", _extends({
      key: provider.meta.id,
      onClick: () => connect(provider.meta.id),
      className: "button-styles ".concat(provider.meta.id, "-styles")
    }, props), /*#__PURE__*/React__default["default"].createElement("img", {
      className: "img-styles",
      src: dark ? provider.meta.icon.dark : provider.meta.icon.light
    }), /*#__PURE__*/React__default["default"].createElement("div", null, /*#__PURE__*/React__default["default"].createElement("span", {
      className: "button-label"
    }, provider.meta.name)));
  }))))) : null;
};

var ConnectDialog$1 = ConnectDialog;

exports.Connect2ICContext = Connect2ICContext;
exports.Connect2ICProvider = Connect2ICProvider;
exports.ConnectButton = ConnectButton$1;
exports.ConnectDialog = ConnectDialog$1;
exports.useBalance = useBalance;
exports.useCanister = useCanister;
exports.useClient = useClient;
exports.useConnect = useConnect;
exports.useDialog = useDialog;
exports.useProviders = useProviders;
exports.useSignMessage = useSignMessage;
exports.useTransfer = useTransfer;
exports.useWallet = useWallet;
