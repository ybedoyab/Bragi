import type { IDL } from "@dfinity/candid";
import { ActorSubclass } from "@dfinity/agent";
export declare const useCanister: (canisterName: string, options?: {
    mode: string;
}) => readonly [ActorSubclass, {
    canisterDefinition: {
        canisterId: string;
        idlFactory: IDL.InterfaceFactory;
    };
    error: boolean;
    loading: boolean;
}];
