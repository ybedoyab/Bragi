import { AstroX } from "./astrox";
import { InternetIdentity } from "./internet-identity";
import { PlugWallet } from "./plug-wallet";
import { StoicWallet } from "./stoic-wallet";
import { InfinityWallet } from "./infinity-wallet";
import { NFID } from "./nfid";
import type { IConnector, IWalletConnector } from "./connectors";
declare type ConnectorClass = {
    new (...args: any[]): IConnector & Partial<IWalletConnector>;
};
export declare type ProviderOptions = {
    connector: ConnectorClass;
    features: Array<string>;
    icon: any;
    id: string;
    name: string;
};
export declare type Provider = {
    connector: IConnector & Partial<IWalletConnector>;
    features: Array<string>;
    icon: any;
    id: string;
    name: string;
};
declare type Config = {
    whitelist: Array<string>;
    host?: string;
    dev?: boolean;
    autoConnect?: boolean;
    providerUrl: string;
    ledgerCanisterId: string;
    ledgerHost?: string;
    appName?: string;
};
export declare const defaultProviders: (config: Config) => Array<Provider>;
export declare const walletProviders: (Config: any) => Array<Provider>;
export { AstroX, InfinityWallet, InternetIdentity, NFID, PlugWallet, StoicWallet, };
