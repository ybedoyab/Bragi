import { ActorSubclass } from "@dfinity/agent";
import type { IConnector } from "./connectors";
import { IDL } from "@dfinity/candid";
declare class StoicWallet implements IConnector {
    #private;
    meta: {
        features: never[];
        icon: {
            light: any;
            dark: any;
        };
        id: string;
        name: string;
    };
    get identity(): any;
    get principal(): string | undefined;
    constructor(userConfig?: {});
    set config(config: {
        whitelist: string[];
        providerUrl: string;
        host: string;
        dev: Boolean;
    });
    get config(): {
        whitelist: string[];
        providerUrl: string;
        host: string;
        dev: Boolean;
    };
    init(): Promise<boolean>;
    createActor<Service>(canisterId: string, idlFactory: IDL.InterfaceFactory): Promise<ActorSubclass<Service> | undefined>;
    isConnected(): Promise<boolean>;
    connect(): Promise<boolean>;
    disconnect(): Promise<boolean>;
}
export { StoicWallet };
