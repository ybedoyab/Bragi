import type { IConnector, IWalletConnector } from "./connectors";
declare class EarthWallet implements IConnector, IWalletConnector {
    #private;
    meta: {
        features: never[];
        icon: {
            light: any;
            dark: any;
        };
        id: string;
        name: string;
    };
    get identity(): any;
    get principal(): string | undefined;
    get ic(): any;
    constructor(userConfig?: {});
    set config(config: {
        whitelist: string[];
        host: string;
        dev: Boolean;
    });
    get config(): {
        whitelist: string[];
        host: string;
        dev: Boolean;
    };
    init(): Promise<boolean>;
    isConnected(): Promise<boolean>;
    createActor(canisterId: any, idlFactory: any): Promise<{}>;
    connect(): Promise<void>;
    disconnect(): Promise<boolean>;
}
export { EarthWallet, };
